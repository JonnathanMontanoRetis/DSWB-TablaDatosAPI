import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.libraryinventory.system',
  appName: 'LibraryInventorySystem',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
